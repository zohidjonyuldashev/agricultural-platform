🌾 Agricultural Platform has been deployed!

1. Get the application URL by running these commands:
{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}
{{- end }}
{{- else if contains "NodePort" .Values.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "agricultural-platform.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else if contains "LoadBalancer" .Values.service.type }}
  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
  kubectl get --namespace {{ .Release.Namespace }} svc -w {{ include "agricultural-platform.fullname" . }}
{{- else if contains "ClusterIP" .Values.service.type }}
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "agricultural-platform.fullname" . }} 8080:{{ .Values.service.port }}
  echo "Visit http://127.0.0.1:8080"
{{- end }}

2. Check application health:
  curl http://127.0.0.1:8080/actuator/health

3. View Prometheus metrics:
  curl http://127.0.0.1:8080/actuator/prometheus

4. API Endpoints:
  - Products: /api/products
  - Orders:   /api/orders
  - Prices:   /api/prices
  - Reports:  /api/reports/daily

📊 Monitoring:
{{- if .Values.serviceMonitor.enabled }}
  ServiceMonitor is enabled - Prometheus will auto-discover this application
{{- else }}
  ServiceMonitor is disabled - configure Prometheus manually to scrape metrics
{{- end }}

🗄️ Database:
{{- if .Values.postgresql.enabled }}
  PostgreSQL is deployed as part of this release
  Host: {{ include "agricultural-platform.postgresqlHost" . }}
{{- else }}
  Using external PostgreSQL at: {{ .Values.env.DB_HOST }}
{{- end }}