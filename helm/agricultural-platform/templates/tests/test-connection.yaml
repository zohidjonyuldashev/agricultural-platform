apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agricultural-platform.fullname" . }}-test-connection"
  labels:
    {{- include "agricultural-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agricultural-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
